pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx,cjs}"
      run: bunx eslint {staged_files}
    types:
      glob: "*.{js,ts,jsx,tsx,cjs}"
      run: bunx tsc
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: bunx commitlint --edit
